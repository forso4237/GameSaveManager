namespace GameSaveManager;

public static class ModHelperData
{
    public const string WorksOnVersion = "55.2";
    public const string Version = "1.0.0";
    public const string Name = "Game Save Manager";

    public const string Description =
        "Save your towers (with upgrades), cash and lives, then reload them into a fresh game. " +
        "Name each save and organize them into folders. Share saves as a single copy-paste code, " +
        "or export/import them as files. F6 = save (asks for a name), F7 = quick-load your latest. " +
        "The on-screen panel is hidden by default — change \"Button location\" in settings to show it. " +
        "Reload on the SAME map + mode you saved on, since tower positions are map-specific.";

    public const string RepoOwner = "forso4237";
    public const string RepoName = "GameSaveManager";
}